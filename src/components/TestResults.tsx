import React from 'react';
import { ThyroidTests } from '../types/medical';

interface TestResultsProps {
  tests: ThyroidTests;
}

export const TestResults: React.FC<TestResultsProps> = () => {
  // Removed rendering of test results section per request
  return null;
};